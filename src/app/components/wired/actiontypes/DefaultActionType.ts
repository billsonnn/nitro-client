import { WiredFurniture } from '../WiredFurniture';
import { ActionType } from './ActionType';

export class DefaultActionType extends WiredFurniture implements ActionType
{
    public get _Str_17249(): boolean
    {
        return true;
    }
}